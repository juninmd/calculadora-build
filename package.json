{
  "name": "calculadora",
  "version": "1.0.0",
  "description": "Sistema de Informação - Calculadora (Express + Angular static build)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:ci": "jest --ci --coverage",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "build": "node scripts/verify-build.js",
    "audit": "npm audit --audit-level=high"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "consolidate": "^0.14.5",
    "express": "^4.15.0",
    "mustache": "^2.3.0"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "jest": "^29.7.0",
    "jest-junit": "^16.0.0",
    "prettier": "^3.3.0",
    "supertest": "^7.0.0"
  }
}
